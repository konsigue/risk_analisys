import { RfcValidatorDirective } from './rfc-validator.directive';

describe('RfcValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new RfcValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
